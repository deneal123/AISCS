# Что НЕ уезжает в контекст сборки.
#
# Раньше файла не было вовсе, и docker получал каталог целиком — вместе с `.venv`
# (100+ МБ) и кэшами. На образ это не влияло (Dockerfile копирует по именам), но
# каждая сборка гнала демону лишние сотни мегабайт.
.venv/
.git/
.ruff_cache/
.pytest_cache/
__pycache__/
*.py[cod]
.coverage
htmlcov/

# Тесты в прод-образ не нужны: там нет ни pytest, ни причины их держать.
tests/
