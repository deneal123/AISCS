import { colors } from '@theme/tokens';

export const WORK_HUB_THEME = {
  pageBg: colors.bg.page,
  panelBg: colors.bg.card,
  inputBg: colors.bg.input,
  headerBg: colors.bg.header,
  panelBorder: colors.border.subtle,
  panelHover: colors.bg.card2,
  accentSoft: colors.accent.soft,
};

export const WORK_SCROLLBAR_SX = {
  scrollbarGutter: 'stable',
};
