repos:
  - repo: local
    hooks:
      - id: presentation-check
        name: presentation sidecar check
        entry: uv run --extra dev --frozen pytest -q
        language: system
        pass_filenames: false
        always_run: true
