"""Backend-половина бывших кросс-сервисных тестов.

Эти тесты писались во времена монолита и проверяли сцепку двух половин сразу: биллинг
поверх стриминга движка, чат-API поверх агента, подстановку модели поверх реестра
провайдеров. Агентская половина уехала в ``agents/tests/cross_service/`` — пакет
сайдкара переименовывается ``gpthub_agents`` → ``service``, после чего два разных
``service`` в одном интерпретаторе не уживаются.

Здесь остались только те проверки, где backend — исполнитель, а не наблюдатель: запись
события биллинга в БД, потолок цены при подмене модели, разбор архива и классификация
залитого файла, чат-API. Что именно НЕ переехало и почему — написано в каждом файле.

⚠️ Прежде тут стоял гейт: «домен недоступен → пропустить весь каталог». Он снят вместе с
последней зависимостью от домена. Оставить его значило бы получить ровно то, от чего он
и защищал, — молчаливый «зелёный»: ни один из оставшихся тестов больше не импортирует
``gpthub_agents``, и пропускать их не за что.
"""
