"""Тесты, переехавшие из ``backend/tests/cross_service/`` — их агентская половина.

Каталог сохранён отдельным намеренно: эти тесты писались во времена монолита и
проверяли сцепку двух сервисов сразу. Держать их вперемешку с юнитами сайдкара
значило бы потерять след — почему у них такая форма и чего в них БОЛЬШЕ НЕТ.

Переезд вынужденный: пакет сайдкара переименовывается ``service`` → ``service``,
после чего он и пакет backend'а — два разных ``service`` — физически не уживаются в
одном интерпретаторе. Значит, backend-половину надо было отцепить ДО переименования.

Большая часть «импортов backend'а» оказалась ДУБЛИКАТАМИ того, чем сайдкар владеет сам
(события, реестр моделей, классификатор дороговизны, настройки) — они просто
перевешены на здешние. Остальное честно осталось в backend'е: см. комментарии в самих
файлах и то, что backend НЕ отдал (SQL биллинга, потолок цены, разбор zip, загрузка
файла) — там движок не участвует, и фейк проверял бы сам себя.

Своих фикстур каталогу не нужно: сброс процесс-глобальных снимков и circuit breaker'а
приезжает из ``tests/conftest.py`` уровнем выше.
"""
