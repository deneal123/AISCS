"""agents — сайдкар агентного движка GPTHub.

Пакет намеренно ПУСТОЙ. Здесь жил ленивый реэкспорт `DefaultAgentExecutionService`
через `__getattr__`, которым не пользовался никто: все потребители импортируют
подмодули напрямую. Реэкспорт на верхнем уровне пакета — это ещё и обязательство
поддерживать вторую точку входа в тот же класс, ради нуля вызовов.
"""
