"""Общее внутри сайдкара: то, чем пользуются разные слои движка.

Не путать с общим МЕЖДУ СЕРВИСАМИ: здесь нет ничего, о чём договариваются backend и
сайдкар. Контракт публикуется через HTTP (`/health`, `/openapi.json`), а не общим кодом.
"""
