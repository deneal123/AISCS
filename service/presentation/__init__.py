"""Слой HTTP: ручки, их авторизация, форма ошибки и загрузка движка.

Здесь и только здесь сервис знает про HTTP. Ниже (`application`, `domain`) о нём не
знают вовсе — иначе движок нельзя было бы вызвать иначе как запросом.

    routers/    сами ручки, сгруппированные по назначению
    deps.py     авторизация внутренних ручек
    errors.py   единая форма ошибки, обработчики фреймворка, лимит тела
    runtime.py  загрузка движка НА СТАРТЕ и ответ «что доступно»
"""
