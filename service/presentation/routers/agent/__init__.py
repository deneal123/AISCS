"""Ядро сервиса: прогон агента и роутинг до диспатча.

⚠️ Обе ручки денежные. `/run` отдаёт терминальный result-dict, из которого воркер
backend СПИСЫВАЕТ токены, а `/route` — `routing_usage` (токены роутер-ЛЛМ) и
`selected_model`, под который резервируются кредиты. Поэтому у них общая группа: их
контракт сверяется офлайн-гейтом суперпроекта, и трогать их вслепую нельзя.
"""
