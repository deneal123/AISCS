"""Возможности движка, выставленные наружу как HTTP-ручки.

Почему ручки, а не дубль реализации у backend: у `parse_url` внутри защита от SSRF,
у `pptx` — ЛЛМ-вызов, у `/vector/*` — эмбеддинги мультипровайдерного слоя. Двум
копиям такого расходиться нельзя (отставшая копия SSRF-гарда — это дыра), поэтому
вызов идёт ТУДА, где защита живёт и поддерживается.
"""
