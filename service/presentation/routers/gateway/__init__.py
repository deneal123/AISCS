"""OpenAI-совместимый шлюз `/v1` — ЧУЖОЙ протокол.

⚠️ Отдельная группа потому, что здесь действуют НЕ НАШИ правила, и это названное
отступление от регламента, а не недосмотр:

* форма ошибки `{"error": {"message", "type"}}` вместо нашей плоской — так её ждут
  memos, ldr, graphify и любые сторонние SDK;
* своя проверка ключа: при пустом значении пускает всех (dev-режим), при выключенном
  `llm_gateway_enabled` отдаёт 404.

⚠️ Второе свойство и есть причина изоляции. Пока эта проверка использовалась и для
внутренних ручек, `/run` (полный контекст пользователя и биллинг) и `/providers/keys`
(приём ключей провайдеров) были ОТКРЫТЫ при незаполненном ключе. Правила шлюза не
должны утекать за пределы этого каталога.
"""
