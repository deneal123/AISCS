"""Домен движка: агенты, мультипровайдерный клиент, пайплайн, роутинг, инструменты.

Пакет намеренно НЕ ре-экспортирует ничего. Раньше здесь стоял агрегатор, и импорт
любого соседа (`from ...domain import media`) тянул за собой `base`, а с ним весь
SDK агентов. Импортировать следует из конкретных модулей.

Контракты событий (`AgentEvent`, `EventType`) живут в общем `service.events` —
их знают обе стороны, и промежуточный ре-экспорт лишь прятал первоисточник.
"""
