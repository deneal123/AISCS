"""Объявленные workflow-агенты.

Новый workflow — это ОДИН модуль здесь плюс строка в источниках реестра. Движка исполнения
у него нет и не будет: шаги отдаются существующему `execute_steps` (см. `WorkflowSpec`).
"""
