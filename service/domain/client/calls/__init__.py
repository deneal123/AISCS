"""Вызовы к провайдерам: как именно платформа их дёргает.

    chat.py       не-стримовые вызовы с ПОЛНЫМ фейловером
    streaming.py  стрим: фейловер только ДО первой дельты
    catalog.py    агрегированный каталог моделей с TTL-кэшем
    retry.py      классификация ошибок, бэкофф, параметры ретраев

⚠️ Разница между `chat` и `streaming` не техническая, а смысловая: в стриме пользователь
уже видит текст, и подмена провайдера после первой дельты склеила бы два разных ответа.
Поэтому фейловер там обрывается на первой дельте, а не «где получится».
"""
