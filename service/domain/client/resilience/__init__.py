"""Отказоустойчивость: кого сейчас нельзя спрашивать и почему.

    provider_policy.py  выключен админом (disabled) либо заблокирован проверкой (blocked)
    circuit_breaker.py  временно упал по трафику (429/таймаут), с остыванием

⚠️ Три состояния РАЗНЫЕ, и смешивать их нельзя. `disabled`/`blocked` — осознанные
решения, они прячут провайдера ВСЕГДА, даже если это опустошит пикер. `breaker` —
временный сигнал из трафика, и у него есть поблажка «не опустошай»: если после отсева
не осталось никого, прячем никого — фейловер подстрахует.
"""
