"""Схемы сайдкара: тела его собственных ручек.

Жили в общем пакете, хотя backend их не импортировал ни разу — он собирает эти
payload'ы руками в своём клиенте (`agents_client/`). То есть «общая» схема
проверялась ровно на одной стороне — на нашей, при разборе запроса, — что даёт ту же
гарантию, что и локальная.
"""
