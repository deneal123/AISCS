"""Инфраструктура движка: интеграции с соседними сервисами (`integration.*`).

Реализации сессий отсюда удалены. `RedisSession`/`SQLiteSession`/`create_session`
доставали клиента из DI-контейнера backend'а, которого в сайдкаре нет, и не
использовались НИ ОДНОЙ из сторон — держались только собственными тестами.
Заодно тот модуль монкипатчил `PseudoSession.add_items`, добавляя валидацию,
которая в бою не применялась никогда (его никто не импортировал).

Живая сессия — `domain.sessions.PseudoSession` (in-memory, самодостаточна).
"""
