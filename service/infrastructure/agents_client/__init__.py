"""Всё, чем BACKEND разговаривает с сайдкаром агентов.

Эти модули лежали внутри ``services/agents/`` — то есть в дереве, которое уехало в
сайдкар. Но принадлежат они backend'у: это его адаптеры (выбор движка, HTTP-клиенты
``/run``, ``/providers/*``, ``/vector/*``, ``/tools/*``) и его же персист сгенерированных
артефактов. Держать их рядом с чужим кодом мешало удалить домен из backend.
"""
