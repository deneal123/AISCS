"""ЗЕРКАЛО видов провайдерских вызовов. Владелец — сайдкар (`agents/service/contracts.py`).

Прогон делает несколько вызовов LLM, и часть из них служебные: выбор модели,
декомпозиция, сжатие контекста, описание вложений. Они идут на дешёвой мета-модели, и
это штатно.

🔴 Признак «ответила не та модель, которую ты выбрал» строился из ЛЮБОГО вызова с
моделью ≠ выбранной — то есть служебный вызов на `gpt-4o-mini` помечал ответ как
подменённый. Замерено живьём: на выбранной `GigaChat-2-Max` ответ дал именно GigaChat,
а бейдж «замена модели» горел, потому что рядом отработала декомпозиция. В разобранном
диалоге он горел на трёх ответах из четырёх — и понять, кто отвечал на самом деле,
стало нельзя.

⚠️ Расхождение с владельцем ловит `scripts/check_contract_parity.py`. Разъедься набор —
и бейдж снова начал бы врать, причём молча.
"""

from __future__ import annotations

SERVICE_USAGE_KINDS = frozenset(
    {
        "route_model",
        "meta_usage",
        "multimodal_usage",
        "image_condense_usage",
        "pptx_illustration_usage",
        "query_resolution",
        "research_plan",
        "tool_selector",
        "audio_transcription",
        "translation",
        "document_visual_audit",
    }
)
