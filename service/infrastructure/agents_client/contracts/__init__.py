"""Копия ПРОВОДА сайдкара на стороне потребителя.

Формат событий и тело `/run` описаны здесь заново, а не импортируются у соседа:
общий класс означал бы, что изменение контракта требует согласованного релиза
обоих сервисов. Совпадение держат стражи: сайдкар публикует имена в `/health`,
офлайн-джоба CI сверяет множества, а `test_contract_parity_with_sidecar`
проверяет живой сервис.

⚠️ `AgentEvent` обязан оставаться терпимым к незнакомым полям (`extra="ignore"`):
сериализатор провайдера шлёт `job_id`/`message`/`timestamp`/`tool_name`, и новая
версия сайдкара вправе добавить своё. Строгая модель роняла бы разбор события.
"""
