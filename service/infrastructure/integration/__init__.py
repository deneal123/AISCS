"""Внешние интеграции агентского домена.

Здесь остаётся ТОЛЬКО то, что нужно самому движку: клиент deep-research (LDR), который
зовёт субагент ``domain/subagents/deep_research``. Клиент долговременной памяти (MemOS)
отсюда УЕХАЛ в ``service/infrastructure/memory``: от агентского домена он не зависел
вовсе, а пользовался им backend (аналитика памяти + сбор ``memory_parts`` для тела
``/run``). Пока он лежал здесь, backend был обязан таскать с собой весь домен — это
последнее, что мешало вынести домен в сайдкар физически (Фаза 5).

Сайдкару клиент памяти и не нужен: память приезжает готовой в теле ``/run`` (Фаза 0b.1).
"""
