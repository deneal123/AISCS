# Submission package

Перед отправкой сюда копируются только утверждённые PDF и требуемые площадкой исходники.
Пакет формируется `pubctl package <PUB-ID> --apply`; его SHA-256 фиксируется отдельно.
Не включайте временные файлы сборки, локальные snapshots и закрытые данные.
